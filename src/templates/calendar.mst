<table class='calendar'>

    <caption>{{monthName}}</caption>

    <thead>
        <tr>
            {{#dayOfWeekNames}}
                <th>{{.}}</th>
            {{/dayOfWeekNames}}
        </tr>
    </thead>

    <tbody>
        {{#weeks}}
            <tr>
                {{#week}}
                    <td class='calendarCell{{#isPrevOrNextMonthDay}} prevOrNextMonthDay{{/isPrevOrNextMonthDay}}{{#hidden}} hidden{{/hidden}}'>
                        <div class='cellWrapper'>
                            <div class='calendarDateNumber'>{{dayNumber}}</div>
                            <div class='calendarCellPrice'><p>{{price}}</p></div>
                            {{!<div class='calendarCellPrice'><p>242.43$</p></div>}}
                        </div>
                    </td>
                {{/week}}
            </tr>
        {{/weeks}}
    </tbody>

</table>