define([
          'angular'
        , 'angular_resource'

    ],
    function (
          angular
        , angular_resource

    ) {
        'use strict';

        return angular.module('sabreDevStudioWebServices', ['ngResource', 'configuration'])
    });
